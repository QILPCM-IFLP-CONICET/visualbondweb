services:
  - type: web
    name: visualbondweb
    runtime: python
    buildCommand: pip install .
    startCommand: uvicorn visualbondweb.api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
